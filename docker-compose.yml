services:
  manual-test-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: multi-agent-data-synthesis
    restart: unless-stopped
    ports:
      - "8527:8000"
    env_file:
      - .env
    volumes:
      - ./outputs:/app/outputs
      - ./data:/app/data
